# Analytics Desk

Dashboard con filtros, charts y scroll infinito. El carrito / sesión viven en Zustand.

```bash
cd analytics-desk
npm i
npm run dev
```

Los métricas salen de un mock local (`src/mocks/metrics.js`) para no depender de un backend real.
